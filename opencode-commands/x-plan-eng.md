---
description: '把工作表中的每項工作定義完整，建立 Issue/Spike/WG 文件、Owner、branch/worktree，準備到可直接執行。'
---

<!-- skill-x-managed-command: x-plan-eng -->
<!-- 由 bin/build.sh 產生，請改 commands-src/x-plan-eng/SKILL.md 後重新 build。 -->

Use the `skill` tool to load the `x-plan-eng` skill, then follow its instructions exactly.
The skill itself is the single source of truth; do not act on any summary of it.

Arguments from the user: $ARGUMENTS

If those arguments are empty, ask the user what they want the skill to work on before continuing.
