---
description: '掃描專案或指定範圍，建立 Cycle、專案總覽與工作總表；找齊工作但不深入實作。'
---

<!-- skill-x-managed-command: x-discovery -->
<!-- 由 bin/build.sh 產生，請改 commands-src/x-discovery/SKILL.md 後重新 build。 -->

Use the `skill` tool to load the `x-discovery` skill, then follow its instructions exactly.
The skill itself is the single source of truth; do not act on any summary of it.

Arguments from the user: $ARGUMENTS

If those arguments are empty, ask the user what they want the skill to work on before continuing.
