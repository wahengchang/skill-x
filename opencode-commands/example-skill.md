---
description: '用來確認 skill-x-starter 安裝是否正常，並示範技能檔案格式。'
---

<!-- skill-x-managed-command: example-skill -->
<!-- 由 bin/build.sh 產生，請改 commands-src/example-skill/SKILL.md 後重新 build。 -->

Use the `skill` tool to load the `example-skill` skill, then follow its instructions exactly.
The skill itself is the single source of truth; do not act on any summary of it.

Arguments from the user: $ARGUMENTS

If those arguments are empty, ask the user what they want the skill to work on before continuing.
