---
description: '完成 final gates、文件同步、獨立 review、commit/push，並 create-or-update 一個 PR。'
---

<!-- skill-x-managed-command: x-ship -->
<!-- 由 bin/build.sh 產生，請改 commands-src/x-ship/SKILL.md 後重新 build。 -->

Use the `skill` tool to load the `x-ship` skill, then follow its instructions exactly.
The skill itself is the single source of truth; do not act on any summary of it.

Arguments from the user: $ARGUMENTS

If those arguments are empty, ask the user what they want the skill to work on before continuing.
