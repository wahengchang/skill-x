---
description: 'Create or reuse a precise GitHub engineering issue, dispatch exactly one Codex Cloud implementation task, and verify the handoff; use when the user asks to delegate repository work to Codex through a GitHub issue.'
---

<!-- skill-x-managed-command: issue-codex -->
<!-- 由 bin/build.sh 產生，請改 commands-src/issue-codex/SKILL.md 後重新 build。 -->

Use the `skill` tool to load the `issue-codex` skill, then follow its instructions exactly.
The skill itself is the single source of truth; do not act on any summary of it.

Arguments from the user: $ARGUMENTS

If those arguments are empty, ask the user what they want the skill to work on before continuing.
